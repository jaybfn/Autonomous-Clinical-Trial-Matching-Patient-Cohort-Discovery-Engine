name: trialmatch
version: 1.0.0
config-version: 2
profile: trialmatch

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
target-path: target
clean-targets: [target, dbt_packages]

models:
  trialmatch:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
    audit:
      +schema: audit
      +materialized: view
      +enabled: true
