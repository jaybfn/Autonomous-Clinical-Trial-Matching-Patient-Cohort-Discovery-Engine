apiVersion: v1
kind: Service
metadata:
  name: trialmatch-api
  namespace: trialmatch
  labels:
    app.kubernetes.io/name: trialmatch-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: trialmatch-api
  ports:
    - name: http
      port: 80
      targetPort: http
