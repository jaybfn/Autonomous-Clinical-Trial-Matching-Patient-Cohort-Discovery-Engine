# `tests/fixtures/synthea/*.README.md`

## Purpose

Tiny Synthea fixtures for unit tests (patients, labs, conditions, clinical_notes). Prefer these over `data/synthea/samples` inside mapper/script unit tests for speed/stability.
